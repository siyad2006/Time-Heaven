{{!--
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Address Page</title>
    <link rel="stylesheet" href="/css/homestyle.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>

<body>
    <nav class="navbar na navbar-expand-lg fixed-top">
        <!-- Navbar content -->
    </nav>

    <!-- Saved Addresses Section -->
    <div class="container mt-5 pt-4">
        <h4 class="text-center mb-4">Saved Addresses</h4>
        <div class="row justify-content-center">

            <!-- Address Dropdown -->
            <label for="addressSelect" class="form-label"><strong>Select Address</strong></label>
            <select id="addressSelect" class="form-select mb-4" required>
                <option value="default">select one </option>
                {{#each address}}
                <option value="{{this._id}}" data-name="{{this.title}}" data-phone="{{this.phone}}"
                    data-street="{{this.houseAddress}}" data-city="{{this.city}}" data-state="{{this.state}}"
                    data-postal="{{this.pincode}}" data-country="{{this.country}}">
                    {{this.title}}, {{this.houseAddress}}, {{this.city}}, {{this.state}} - {{this.pincode}}
                </option>
                {{/each}}
            </select>

            <!-- Address Form -->
            <div class="container-fluid edit-container justify-content-center">
                <div class="edit-card">
                    <h4 class="text-center">Add New Address</h4>

                    <!-- Form for Address Details -->
                    <form id="placeorder">
                        <input type="hidden" value="{{userId}}" id="userid">
                        <div class="mb-3">
                            <label for="name" class="form-label"><strong>Name</strong></label>
                            <input type="text" class="form-control" id="name" placeholder="name">
                        </div>
                        <div class="mb-3">
                            <label for="phone" class="form-label"><strong>Phone</strong></label>
                            <input type="text" class="form-control" id="phone" placeholder="+91">
                        </div>
                        <div class="mb-3">
                            <label for="street" class="form-label"><strong>Street Address</strong></label>
                            <input type="text" class="form-control" id="street" placeholder="123 Main St">
                        </div>
                        <div class="mb-3">
                            <label for="city" class="form-label"><strong>City</strong></label>
                            <input type="text" class="form-control" id="city" placeholder="City">
                        </div>
                        <div class="mb-3">
                            <label for="state" class="form-label"><strong>State</strong></label>
                            <input type="text" class="form-control" id="state" placeholder="State">
                        </div>
                        <div class="mb-3">
                            <label for="postalCode" class="form-label"><strong>Postal Code</strong></label>
                            <input type="text" class="form-control" id="postalCode" placeholder="Postal Code">
                        </div>
                        <div class="mb-3">
                            <label for="country" class="form-label"><strong>Country</strong></label>
                            <input type="text" class="form-control" id="country" placeholder="Country">
                        </div>
                        <div class="d-flex justify-content-center">
                            <button type="submit" class="btn btn-save">Go to Payment</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->

    <footer class="bg-dark text-white mt-5">
        <div class="container py-4">
            <div class="row">
                <div class="col-md-4">
                    <h5>About Us</h5>
                    <p>TimeHaven is your go-to destination for stylish and affordable watches. We believe in
                        offering quality products with exceptional service.</p>
                </div>
                <div class="col-md-4">
                    <h5>Customer Service</h5>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-white">Contact Us</a></li>
                        <li><a href="#" class="text-white">Returns</a></li>
                        <li><a href="#" class="text-white">Shipping Information</a></li>
                        <li><a href="#" class="text-white">Privacy Policy</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h5>Follow Us</h5>
                    <ul class="list-unstyled d-flex">
                        <li class="me-3">
                            <a href="#" class="text-white">
                                <i class="bi bi-facebook"></i>
                            </a>
                        </li>
                        <li class="me-3">
                            <a href="#" class="text-white">
                                <i class="bi bi-twitter"></i>
                            </a>
                        </li>
                        <li class="me-3">
                            <a href="#" class="text-white">
                                <i class="bi bi-instagram"></i>
                            </a>
                        </li>
                        <li>
                            <a href="#" class="text-white">
                                <i class="bi bi-linkedin"></i>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
            <hr class="my-4 bg-white">
            <div class="text-center">
                <p>&copy; 2024 TimeHaven. All rights reserved.</p>
            </div>
        </div>
    </footer>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>

    <!-- JavaScript to Populate Form Fields -->
    <script>
        document.getElementById('addressSelect').addEventListener('change', function () {
            const selectedOption = this.options[this.selectedIndex];

            // Populate form fields with selected address data
            document.getElementById('name').value = selectedOption.getAttribute('data-name') || '';
            document.getElementById('phone').value = selectedOption.getAttribute('data-phone') || '';
            document.getElementById('street').value = selectedOption.getAttribute('data-street') || '';
            document.getElementById('city').value = selectedOption.getAttribute('data-city') || '';
            document.getElementById('state').value = selectedOption.getAttribute('data-state') || '';
            document.getElementById('postalCode').value = selectedOption.getAttribute('data-postal') || '';
            document.getElementById('country').value = selectedOption.getAttribute('data-country') || '';
        });
    </script>
    <script>
        document.getElementById('placeorder').addEventListener('submit', async (event) => {
            event.preventDefault();
            let user = document.getElementById('userid').value;
            const formData = {
                // Gather form data, e.g., using FormData API or manually
                name: document.getElementById('name').value,
                phone: document.getElementById('phone').value,
                street: document.getElementById('street').value,
                city: document.getElementById('city').value,
                state: document.getElementById('state').value,
                postalCode: document.getElementById('postalCode').value,
                country: document.getElementById('country').value
            };

            try {
                const response = await fetch(`/user/placeorder/${user}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    Swal.fire({
                        title: 'Success!',
                        text: 'Your checkout details have been saved.',
                        icon: 'success',
                        confirmButtonText: 'OK'
                    }).then(() => window.location.href = '/user/login')
                } else {
                    throw new Error('Failed to save data.');
                }
            } catch (error) {
                Swal.fire({
                    title: 'Error',
                    text: 'There was an issue saving your checkout details. Please try again.',
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
            }
        });
    </script>
</body>

</html> --}}


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout</title>
    <link rel="stylesheet" href="/css/homestyle.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        .container {
            max-width: 1200px;
        }

        .checkout-section {
            margin-top: 80px;
        }

        .form-check-label {
            font-size: 16px;
        }

        .form-label {
            font-weight: bold;
        }

        .btn-custom {
            background-color: #007bff;
            color: white;
            width: 100%;
        }
    </style>
</head>

<body>
    <nav class="navbar na navbar-expand-lg fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand text-white ms-2 head-logo" href="#">
                <img src="/img/download copy.png" alt="Logo" width="40" height="34"
                    class="d-inline-block align-text-top"> TimeHaven
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/user/home">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="#">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/user/shop">Shopping</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="#">Contact Us</a>
                    </li>
                </ul>

                <div class="d-flex align-items-center">
                    <a href="#" class="text-white ms-3">
                        <i class="bi bi-search"></i>
                    </a>
                    <a href="#" class="text-white ms-3">
                        <i class="bi bi-heart"></i>
                    </a>
                    <a href="/user/cart/{{userid}}" class="text-white ms-3">
                        <i class="bi bi-cart"></i>
                    </a>
                    <a href="/user/profile" class="text-white ms-3">
                        <i class="bi bi-person"></i>
                    </a>
                    
                </div>
            </div>
        </div>
    </nav>


    <div class="container checkout-section">
        <h2 class="text-center mb-4">Checkout</h2>
        <div class="row">
            <!-- Left Side: Cart Products -->
            <div class="col-md-6">
                <h4>Cart Products</h4>
                <div class="list-group">
                    {{!-- {{#each cartProducts}}
                    <div class="list-group-item d-flex justify-content-between">

                        <div>
                            <h5>{{this.name}}</h5>
                            <p>{{this.description}}</p>
                        </div>
                        <div>
                            <p>Qty: {{this.qty}}</p>
                            <p>Price: ${{this.regularprice}}</p>
                        </div>
                    </div>
                    {{/each}} --}}

                    {{#each cartProducts}}
                    <div class="list-group-item d-flex justify-content-between">
                        <!-- Store product IDs as data attributes for retrieval -->
                        <div class="product-item" data-product-id="{{this._id}}" data-qty="{{this.qty}}">
                            <div>
                                <h5>{{this.name}}</h5>
                                <p>{{this.description}}</p>
                            </div>
                            <div>
                                <p>Qty: {{this.qty}}</p>
                                <p>Price: ${{this.regularprice}}</p>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
                <h5 class="text-end" style="color: rgb(196, 58, 58);">Total : {{cartItem.totalAmount}}</h5>
            </div>

            <!-- Right Side: Address Form and Payment Options -->
            <div class="col-md-6">
                <h4>Billing Information</h4>
                <form id="checkoutForm">
                    <input type="hidden" value="{{userId}}" id="userid">

                    <!-- Name Input -->
                    <div class="mb-3">
                        <label for="name" class="form-label">Full Name</label>
                        <input type="text" class="form-control" id="name" required>
                    </div>

                    <!-- Phone Input -->
                    <div class="mb-3">
                        <label for="phone" class="form-label">Phone Number</label>
                        <input type="text" class="form-control" id="phone" required>
                    </div>

                    <!-- Address Dropdown -->
                    <div class="mb-3">
                        <label for="addressSelect" class="form-label"><strong>Select Address</strong></label>
                        <select id="addressSelect" class="form-select mb-4" required>
                            <option value="default">Select Address</option>
                            {{#each address}}
                            <option value="{{this._id}}" data-name="{{this.title}}" data-phone="{{this.phone}}"
                                data-street="{{this.houseAddress}}" data-city="{{this.city}}"
                                data-state="{{this.state}}" data-postal="{{this.pincode}}"
                                data-country="{{this.country}}">
                                {{this.title}}, {{this.houseAddress}}, {{this.city}}, {{this.state}} - {{this.pincode}}
                            </option>
                            {{/each}}
                        </select>
                    </div>

                    <!-- Additional Address Information -->
                    <div class="mb-3">
                        <label for="street" class="form-label">Street Address</label>
                        <input type="text" class="form-control" id="street" required>
                    </div>

                    <div class="mb-3">
                        <label for="city" class="form-label">City</label>
                        <input type="text" class="form-control" id="city" required>
                    </div>

                    <div class="mb-3">
                        <label for="state" class="form-label">State</label>
                        <input type="text" class="form-control" id="state" required>
                    </div>

                    <div class="mb-3">
                        <label for="postalCode" class="form-label">Postal Code</label>
                        <input type="text" class="form-control" id="postalCode" required>
                    </div>

                    <div class="mb-3">
                        <label for="country" class="form-label">Country</label>
                        <input type="text" class="form-control" id="country" required>
                    </div>

                    <!-- Payment Options -->
                    <h5>Select Payment Method</h5>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="paymentMethod" id="cod" value="cod" required>
                        <label class="form-check-label" for="cod">Cash on Delivery (COD)</label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="paymentMethod" id="paypal" value="paypal"
                            required>
                        <label class="form-check-label" for="paypal">PayPal</label>
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="radio" name="paymentMethod" id="razorpay" value="razorpay"
                            required>
                        <label class="form-check-label" for="razorpay">Razorpay</label>
                    </div>

                    {{!-- <input type="hidden" value="cod" id="payment"> --}}

                    <!-- Submit Button -->
                    <div class="d-flex justify-content-center mt-4">
                        <button type="submit" class="btn btn-custom btn-lg">Place Order</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white mt-5">
        <div class="container py-4">
            <div class="row">
                <div class="col-md-4">
                    <h5>About Us</h5>
                    <p>TimeHaven is your go-to destination for stylish and affordable watches. We believe in offering
                        quality products with exceptional service.</p>
                </div>
                <div class="col-md-4">
                    <h5>Customer Service</h5>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-white">Contact Us</a></li>
                        <li><a href="#" class="text-white">Returns</a></li>
                        <li><a href="#" class="text-white">Shipping Information</a></li>
                        <li><a href="#" class="text-white">Privacy Policy</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h5>Follow Us</h5>
                    <ul class="list-unstyled d-flex">
                        <li class="me-3"><a href="#" class="text-white"><i class="bi bi-facebook"></i></a></li>
                        <li class="me-3"><a href="#" class="text-white"><i class="bi bi-twitter"></i></a></li>
                        <li class="me-3"><a href="#" class="text-white"><i class="bi bi-instagram"></i></a></li>
                        <li><a href="#" class="text-white"><i class="bi bi-linkedin"></i></a></li>
                    </ul>
                </div>
            </div>
            <hr class="my-4 bg-white">
            <div class="text-center">
                <p>&copy; 2024 TimeHaven. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- SweetAlert -->
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>

    <!-- JavaScript to Populate Form Fields -->
    <script>
        document.getElementById('addressSelect').addEventListener('change', function () {
            const selectedOption = this.options[this.selectedIndex];
            document.getElementById('name').value = selectedOption.getAttribute('data-name') || '';
            document.getElementById('phone').value = selectedOption.getAttribute('data-phone') || '';
            document.getElementById('street').value = selectedOption.getAttribute('data-street') || '';
            document.getElementById('city').value = selectedOption.getAttribute('data-city') || '';
            document.getElementById('state').value = selectedOption.getAttribute('data-state') || '';
            document.getElementById('postalCode').value = selectedOption.getAttribute('data-postal') || '';
            document.getElementById('country').value = selectedOption.getAttribute('data-country') || '';
        });
    </script>

    {{!-- <script>
        document.getElementById('checkoutForm').addEventListener('submit', async (event) => {
            event.preventDefault();

            const productElements = document.querySelectorAll('.product-item');
            {{!-- const productIds = Array.from(productElements).map(product => ({
                product.dataset.productId

            })); --}}
               const productIds = Array.from(productElements).map(product => product.dataset.productId);

            console.log(productIds)


            let user = document.getElementById('userid').value;
            const formData = {
                name: document.getElementById('name').value,
                phone: document.getElementById('phone').value,
                street: document.getElementById('street').value,
                city: document.getElementById('city').value,
                state: document.getElementById('state').value,
                postalCode: document.getElementById('postalCode').value,
                country: document.getElementById('country').value,
                paymentMethod: document.querySelector('input[name="paymentMethod"]:checked').value,
                products: productIds

            };

            try {
                const response = await fetch(`/user/placeorder/${user}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    Swal.fire({
                        title: 'Success!',
                        text: 'Your checkout details have been saved.',
                        icon: 'success',
                        confirmButtonText: 'OK'
                    }).then(() => window.location.href = '/user/login');
                } else {
                    throw new Error('Failed to save data.');
                }
            } catch (error) {
                Swal.fire({
                    title: 'Error',
                    text: 'There was an issue saving your checkout details. Please try again.',
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
            }
        });
    </script> --}}
<script>
document.getElementById('checkoutForm').addEventListener('submit', async (event) => {
    event.preventDefault();

    const name = document.getElementById('name').value;
    const phone = document.getElementById('phone').value;
    const postalCode = document.getElementById('postalCode').value;

    // Validation for name
    const namePattern = /^[A-Za-z\s]+$/;
    if (!namePattern.test(name)) {
        Swal.fire({
            title: 'Error',
            text: 'Name can only contain alphabets and spaces.',
            icon: 'error',
            confirmButtonText: 'OK'
        });
        return;  
    }

    // Validation for phone number
    const phonePattern = /^\d{10}$/;
    if (!phonePattern.test(phone)) {
        Swal.fire({
            title: 'Error',
            text: 'Phone number must be exactly 10 digits.',
            icon: 'error',
            confirmButtonText: 'OK'
        });
        return; 
    }

    // Validation for postal code
    const postalCodePattern = /^\d{6}$/;
    if (!postalCodePattern.test(postalCode)) {
        Swal.fire({
            title: 'Error',
            text: 'Postal code must be exactly 6 digits.',
            icon: 'error',
            confirmButtonText: 'OK'
        });
        return;  
    }

    // Collect products and their quantities
    const productElements = document.querySelectorAll('.product-item');
    const products = Array.from(productElements).map(product => {
        return {
            productId: product.dataset.productId,
            qty: parseInt(product.dataset.qty)  // Ensure qty is a number
        };
    });

    console.log(products);  // Log products to check the structure

    let user = document.getElementById('userid').value;
    const formData = {
        name: name,
        phone: phone,
        street: document.getElementById('street').value,
        city: document.getElementById('city').value,
        state: document.getElementById('state').value,
        postalCode: postalCode,
        country: document.getElementById('country').value,
        paymentMethod: document.querySelector('input[name="paymentMethod"]:checked').value,
        products: products,
        country:document.getElementById('country').value  // Send products with productId and qty
    };

    try {
        const response = await fetch(`/user/placeorder/${user}`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(formData)
        });

        if (response.ok) {
            Swal.fire({
                title: 'Success!',
                text: 'Your checkout details have been saved.',
                icon: 'success',
                confirmButtonText: 'OK'
            }).then(() => window.location.href = '/user/login');
        } else {
            throw new Error('Failed to save data.');
        }
    } catch (error) {
        Swal.fire({
            title: 'Error',
            text: 'There was an issue saving your checkout details. Please try again.',
            icon: 'error',
            confirmButtonText: 'OK'
        });
    }
});
</script>

</body>

</html>